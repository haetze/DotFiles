git commit -m "OS runtime test Update" -a 

commits changes and adds all added files

git push http://github.com/haetze/DemoGO master
pushes the file to a git server 

git remote add origin https://github.com/haetze/DemoGO.git


git push -u origin master

curl -u 'USER' https://api.github.com/user/repos -d '{"name":"REPO"}'

# Remember replace USER with your username and REPO with your repository/application name!



alias bat='upower -i /org/freedesktop/UPower/devices/battery_BAT0| grep -E "state|to\full|percentage"'

chmod u+x file

sudo ifconfig wlan0 up

sudo wpa_supplicant -Dwext -iwlan0 -cwpa.conf -B

sudo dhclient -r

sudo dhclient wlan0


sudo fdisk /dev/sdb 

sudo mount -t /dev/sdb1 /media/Volumes/

/etc/init.d/cups start
#port 631 http://127.0.0.1:631
